import { Column, Entity } from 'typeorm'
import { BaseEntity } from '@/libs/common/src/database/base.entity'

@Entity('<%= moduleName %>')
export class <%= CapitalizeModuleName %>Entity extends BaseEntity {
  @Column()
  name: string
}
