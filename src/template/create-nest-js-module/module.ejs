import { Module } from '@nestjs/common'
import { TypeOrmModule } from '@nestjs/typeorm'
import { <%= upperCamelCase %>Entity } from './entities/<%= moduleName %>.entity'
import { <%= upperCamelCase %>Controller } from './<%= moduleName %>.controller'
import { <%= upperCamelCase %>Service } from './<%= moduleName %>.service'

@Module({
  imports: [
    TypeOrmModule.forFeature([
      <%= upperCamelCase %>Entity,
    ]),
  ],
  controllers: [<%= upperCamelCase %>Controller],
  providers: [<%= upperCamelCase %>Service],
  exports: [<%= upperCamelCase %>Service],
})
export class <%= upperCamelCase %>Module {}
